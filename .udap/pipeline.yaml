version: 1
name: gitops-delivery-platform
vcs: github

stages:
  - id: test
    kind: test
    needs: []
    timeout_minutes: 20
    steps:
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Unit tests
        run: mvn -B -ntp verify
        working-directory: app

  - id: security
    kind: security
    needs: []
    timeout_minutes: 20
    steps:
      # OWASP Dependency-Check was REMOVED on 2026-09-05 at the user's request:
      # it downloads and unpacks the full NVD CVE feed on a cold runner cache,
      # routinely costing 5-15 minutes. Trivy covers the same Maven dependency
      # tree AND the container base image (which Dependency-Check never looked
      # at) in well under a minute. The Set up JDK step went with it - it existed
      # only to run the Maven plugin; Trivy scans the filesystem and needs no JDK.
      # The FAIL GATE IS UNCHANGED: HIGH/CRITICAL findings still fail the build.
      - name: Filesystem and config scan (Trivy)
        run: |
          set -euo pipefail
          # Trivy runs from its OFFICIAL CONTAINER IMAGE rather than a
          # marketplace action. A `uses:` reference is resolved by GitHub before
          # any step executes, so a tag that does not exist kills the whole job
          # during action resolution with no scan attempted. An image tag is
          # verified at pull time by the registry and fails with a clear error.
          TRIVY_IMAGE="aquasec/trivy:0.58.1"
          # --ignorefile points at .trivyignore.yaml, which carries SCOPED and
          # EXPIRING exceptions with written justifications. It holds exactly two
          # entries (AVD-AWS-0040 and AVD-AWS-0041 - two views of one fact: the
          # EKS public endpoint, which cannot be closed without a self-hosted
          # runner inside the VPC). The severity gate below is NOT relaxed.
          docker run --rm \
            -v "${GITHUB_WORKSPACE}:/workspace" \
            -v "${HOME}/.cache/trivy:/root/.cache/trivy" \
            "$TRIVY_IMAGE" \
            filesystem \
            --scanners vuln,secret,misconfig \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --ignorefile /workspace/.trivyignore.yaml \
            --exit-code 1 \
            --no-progress \
            /workspace

  # PROVISION RUNS BEFORE IMAGE.
  #
  # It used to run after, which forced the image stage to create the ECR
  # repository imperatively with `aws ecr create-repository` so it had somewhere
  # to push. That gave ONE resource TWO owners: the CLI created it, then
  # aws_ecr_repository.app tried to create it again and terraform failed with
  # RepositoryAlreadyExistsException - every run, permanently, because the CLI
  # always won the race.
  #
  # Terraform is now the SOLE owner of the registry (as it already is of every
  # other AWS resource here), and the image stage READS the repository URL from
  # terraform state. Ordering provision first costs nothing: the cluster has to
  # exist before configure can deploy to it either way.
  - id: provision
    kind: provision
    needs: [test, security]
    timeout_minutes: 45
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      # The source CIDR allowlist for the Argo CD UI, in HCL list syntax.
      # Held as a SECRET rather than committed: this repository is public, and
      # an operator's home IP handed to a scanner is a gift. terraform's
      # variable has no default and rejects an EMPTY list, so a missing value
      # fails the apply rather than quietly rendering an unrestricted rule.
      #
      # As of 2026-09-05 this is ["0.0.0.0/0"] by explicit operator decision -
      # see the ACCEPTED RISK note on the variable in infra/variables.tf.
      TF_VAR_argocd_allowed_cidrs: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
    steps:
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8
      - name: Terraform init
        run: |
          terraform init -input=false -reconfigure \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ secrets.PROJECT_NAME }}/terraform.tfstate" \
            -backend-config="region=us-east-1"
        working-directory: infra
      - name: Adopt a pre-existing ECR repository into state
        run: |
          set -euo pipefail
          # Recovery path for state created before terraform owned the registry.
          # A previous pipeline revision created the repository with the AWS CLI,
          # so it exists in the account but not in state. Import it once; on a
          # clean account both branches are no-ops and terraform creates it.
          if terraform state show aws_ecr_repository.app >/dev/null 2>&1; then
            echo "aws_ecr_repository.app is already in state - nothing to adopt"
          elif aws ecr describe-repositories --region "$AWS_REGION" \
                 --repository-names "$TF_VAR_project_name" >/dev/null 2>&1; then
            echo "repository exists in AWS but not in state - importing"
            terraform import -input=false aws_ecr_repository.app "$TF_VAR_project_name"
          else
            echo "no pre-existing repository - terraform will create it"
          fi
        working-directory: infra
      - name: Terraform validate
        run: terraform validate
        working-directory: infra
      - name: Terraform apply
        run: terraform apply -input=false -auto-approve
        working-directory: infra
      - name: Report the ACM certificate status and validation record
        run: |
          set -euo pipefail
          # The certificate is requested but NOT waited on (see infra/acm.tf):
          # DNS for this zone lives in cPanel, so terraform cannot write the
          # validation record and aws_acm_certificate_validation would block
          # the apply for hours. Print what the operator needs instead.
          STATUS="$(terraform output -raw acm_certificate_status)"
          echo "ACM certificate status: $STATUS"
          if [ "$STATUS" = "ISSUED" ]; then
            echo "Certificate is issued - the Argo CD ingress will be created by configure."
            exit 0
          fi
          NAME="$(terraform output -raw acm_validation_name)"
          VALUE="$(terraform output -raw acm_validation_value)"
          HOSTNAME="$(terraform output -raw argocd_hostname)"
          ZONE="${HOSTNAME#*.}"
          SHORT="${NAME%.${ZONE}.}"
          echo "==================================================================="
          echo "ACTION REQUIRED - add this CNAME in the cPanel Zone Editor"
          echo "==================================================================="
          echo "Record type                 : CNAME"
          echo "Fully-qualified record name : ${NAME}"
          echo "Record value                : ${VALUE}"
          echo
          echo "cPanel usually APPENDS the zone (${ZONE}) to whatever you type."
          echo "If so, enter ONLY this in the Name field:"
          echo "  ${SHORT}"
          echo
          echo "Verify afterwards with:"
          echo "  dig +short CNAME ${NAME}"
          echo "It must return exactly: ${VALUE}"
          echo
          echo "A doubled zone suffix (…${ZONE}.${ZONE}) is the usual cPanel"
          echo "mistake and makes validation silently never complete."
          echo "==================================================================="
        working-directory: infra

  # ---------------------------------------------------------------------------
  # IMAGE — build, push, and COMMIT THE NEW VERSION TO GIT.
  #
  # This stage is where the GitOps loop is closed. It pushes the image, then
  # writes the tag into gitops/apps/values.yaml and commits it. Argo CD then
  # deploys that tag because it is the DESIRED STATE in the repository - not
  # because CI reached into the cluster and patched it there.
  #
  # The previous design patched the tag onto the live Argo Applications with
  # `kubectl patch` while Git declared an empty parameters list. Cluster and Git
  # disagreed by construction, root's selfHeal raced CI on every deploy, and the
  # race was lost on 2026-09-05. Writing to Git removes the race rather than
  # exempting it.
  # ---------------------------------------------------------------------------
  - id: image
    kind: build_push
    needs: [provision]
    timeout_minutes: 30
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_argocd_allowed_cidrs: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
    # The commit the write-back created. configure pins the root Application's
    # targetRevision to it.
    #
    # SAFE AS A JOB OUTPUT: a git SHA is a hex digest that contains no secret
    # substring, so GitHub will not drop it (unlike the ECR URL, which embeds
    # PROJECT_NAME and must be re-read from terraform state in each job).
    outputs:
      gitops_sha: ${{ steps.writeback.outputs.gitops_sha }}
    steps:
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8
      - name: Build application jar
        run: mvn -B -ntp -DskipTests package
        working-directory: app
      - name: Read the ECR repository URL from terraform state
        run: |
          set -euo pipefail
          # Self-sufficient job: re-init with the SAME backend flags and read the
          # value from state rather than threading it through job outputs. The
          # repository URL embeds PROJECT_NAME, which is a secret, so GitHub
          # would silently DROP it as a job output.
          terraform init -input=false -reconfigure \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ secrets.PROJECT_NAME }}/terraform.tfstate" \
            -backend-config="region=us-east-1"
          terraform output -raw ecr_repository_url > /tmp/ecr_url
        working-directory: infra
      - name: Log in to Amazon ECR
        run: |
          set -euo pipefail
          REGISTRY="$(cut -d/ -f1 /tmp/ecr_url)"
          aws ecr get-login-password --region "$AWS_REGION" \
            | docker login --username AWS --password-stdin "$REGISTRY"
      - name: Build and push image tagged with commit SHA
        run: |
          set -euo pipefail
          IMAGE="$(cat /tmp/ecr_url):${GITHUB_SHA}"
          # The ECR repository is IMMUTABLE-tagged: a tag can never be rewritten
          # under a running Deployment. That also means re-running this pipeline
          # on the SAME commit must be a no-op rather than a failure.
          if aws ecr describe-images --region "$AWS_REGION" \
               --repository-name "$PROJECT_NAME" \
               --image-ids imageTag="$GITHUB_SHA" >/dev/null 2>&1; then
            echo "image for $GITHUB_SHA already present in ECR - skipping build and push"
            exit 0
          fi
          docker build -t "$IMAGE" app
          docker push "$IMAGE"
          echo "pushed image for $GITHUB_SHA"
      - name: Write the new image tag into gitops/apps/values.yaml and commit
        id: writeback
        env:
          # The built-in token, scoped to this repository. The REPOSITORY's
          # "Workflow permissions" setting must be "Read and write" - the
          # pipeline spec has no field for a job-level `permissions:` block.
          # See ADR-003 in .udap/notes.md.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          ECR_URL="$(cat /tmp/ecr_url)"
          # REWRITTEN WHOLESALE WITH A HEREDOC, not edited in place with sed.
          # The ECR URL contains slashes and dots; a sed s/// expression over it
          # is a delimiter bug waiting to happen (known pitfall #11). The file is
          # small and fully owned by this stage, so emitting it entire is both
          # safer and easier to read in a diff.
          #
          # The delimiter is UNQUOTED so ${ECR_URL} and ${GITHUB_SHA} expand;
          # every literal $ and backtick in the body is therefore escaped.
          cat > gitops/apps/values.yaml <<EOF
          # ---------------------------------------------------------------------------
          # APP-OF-APPS VALUES — the DESIRED STATE of every environment.
          #
          # GENERATED AND COMMITTED BY CI (the image stage, and the rollback
          # workflow). Edit by hand only to pin or roll a version deliberately —
          # Argo CD reconciles whatever is committed here.
          #
          # The image tag lives in GIT, not in a kubectl patch against the live
          # Applications. That is what lets root's selfHeal stay on permanently:
          # cluster state and Git state agree, so there is nothing to fight and
          # no ignoreDifferences exemption hiding a disagreement.
          # ---------------------------------------------------------------------------
          repoURL: ""

          targetRevision: main
          chartPath: chart/gitops-delivery-platform

          # The ECR repository, from: terraform output -raw ecr_repository_url
          imageRepository: "${ECR_URL}"

          # PER-ENVIRONMENT IMAGE TAGS (commit SHAs). Separate keys so a rollback
          # can move ONE environment back without dragging the others with it.
          imageTagDev: "${GITHUB_SHA}"
          imageTagBluegreen: "${GITHUB_SHA}"
          imageTagCanary: "${GITHUB_SHA}"
          EOF
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # IDEMPOTENT. Re-running the pipeline on the SAME commit produces an
          # identical file; `git commit` with nothing staged exits non-zero and
          # would fail the job. Detect that and reuse the current HEAD instead.
          if git diff --quiet -- gitops/apps/values.yaml; then
            echo "gitops/apps/values.yaml already names ${GITHUB_SHA} - no commit needed"
            GITOPS_SHA="$(git rev-parse HEAD)"
          else
            git add gitops/apps/values.yaml
            git commit -m "deploy: set image tag to ${GITHUB_SHA}"
            # Rebase onto any concurrent commit rather than force-pushing over
            # it. Deploys are workflow_dispatch-only and single-writer, so this
            # is belt and braces, not an expected path.
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push origin "HEAD:${GITHUB_REF_NAME}"
            GITOPS_SHA="$(git rev-parse HEAD)"
            echo "committed ${GITOPS_SHA}"
          fi
          echo "gitops_sha=${GITOPS_SHA}" >> "$GITHUB_OUTPUT"

  - id: configure
    kind: configure
    needs: [provision, image]
    timeout_minutes: 45
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_argocd_allowed_cidrs: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
      PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
      GITOPS_REPO_URL: ${{ github.server_url }}/${{ github.repository }}
      ARGOCD_ALLOWED_CIDRS: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
      ARGOCD_ADMIN_PASSWORD: ${{ secrets.ARGOCD_ADMIN_PASSWORD }}
      # The commit the image stage wrote the new tag into. This job checked out
      # that commit's PARENT (the SHA that triggered the run), so the root
      # Application must be pointed at the write-back SHA explicitly - otherwise
      # Argo would sync a revision whose values.yaml still names the previous
      # image, and the deploy would only converge if Argo happened to poll main
      # later. Pinning makes it deterministic.
      GITOPS_TARGET_REVISION: ${{ needs.image.outputs.gitops_sha }}
      # Argo Rollouts controller version. Pinned for the same reason every chart
      # here is pinned: an unpinned "latest" manifest changes the CRD schema
      # under a running cluster with no commit to point at.
      ARGO_ROLLOUTS_VERSION: v1.7.2
    steps:
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8
      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.16.3
      - name: Set up kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.33.0
      - name: Read infrastructure outputs from terraform state
        id: tf
        run: |
          set -euo pipefail
          terraform init -input=false -reconfigure \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ secrets.PROJECT_NAME }}/terraform.tfstate" \
            -backend-config="region=us-east-1"
          terraform output -raw cluster_name > /tmp/cluster_name
          terraform output -raw alb_controller_role_arn > /tmp/albc_role_arn
          terraform output -raw ecr_repository_url > /tmp/ecr_url
          terraform output -raw vpc_id > /tmp/vpc_id
          terraform output -raw acm_certificate_arn > /tmp/acm_arn
          terraform output -raw acm_certificate_status > /tmp/acm_status
          terraform output -raw argocd_hostname > /tmp/argocd_hostname
        working-directory: infra
      - name: Update kubeconfig
        run: |
          set -euo pipefail
          aws eks update-kubeconfig --region "$AWS_REGION" --name "$(cat /tmp/cluster_name)"
          kubectl cluster-info
      - name: Install or upgrade Argo CD
        run: |
          set -euo pipefail
          helm repo add argo https://argoproj.github.io/argo-helm
          helm repo update
          kubectl create namespace argocd --dry-run=client -o yaml | kubectl apply -f -
          helm upgrade --install argocd argo/argo-cd \
            --namespace argocd \
            --version 7.7.11 \
            --set configs.params."server\.insecure"=true \
            --wait --timeout 15m
      - name: Install or upgrade the Argo Rollouts controller
        run: |
          set -euo pipefail
          # WHY THIS RUNS BEFORE THE ROOT APPLICATION IS APPLIED.
          #
          # app-bluegreen and app-canary render a `kind: Rollout`. A CRD that
          # does not exist yet makes the API server REJECT the object, and Argo
          # CD surfaces that as a sync failure on the child Application - which
          # looks identical to a broken chart. Installing the controller first
          # means the CRDs are registered before anything tries to create one.
          #
          # INSTALLED WITH kubectl, NOT HELM, and deliberately so: this is the
          # upstream release manifest (the install path Argo Rollouts documents
          # first), it carries its own CRDs, and there is no values file worth
          # maintaining for a stock controller. Keeping it out of Helm also
          # keeps it out of any future `helm list` cleanup that walks releases.
          #
          # IDEMPOTENT: `create namespace --dry-run=client | apply` is the
          # standard create-or-noop, and --server-side apply reconciles an
          # existing install in place rather than erroring on conflict.
          # --force-conflicts lets a later version take ownership of fields a
          # previous client-side apply wrote, which is the usual upgrade snag.
          #
          # The CRDs are LARGE (well past the 262144-byte annotation limit that
          # client-side apply imposes), so --server-side is required here, not
          # merely preferred.
          kubectl create namespace argo-rollouts --dry-run=client -o yaml | kubectl apply -f -
          kubectl apply --server-side --force-conflicts \
            -n argo-rollouts \
            -f "https://github.com/argoproj/argo-rollouts/releases/download/${ARGO_ROLLOUTS_VERSION}/install.yaml"
          # Block until the CRD is actually registered and the controller is
          # serving. Applying a Rollout against a half-registered CRD fails with
          # "no matches for kind", which is a confusing way to learn about a
          # race that a single wait removes.
          kubectl wait --for=condition=established --timeout=120s \
            crd/rollouts.argoproj.io
          kubectl wait --for=condition=established --timeout=120s \
            crd/analysistemplates.argoproj.io
          kubectl -n argo-rollouts rollout status deployment/argo-rollouts --timeout=5m
          echo "Argo Rollouts ${ARGO_ROLLOUTS_VERSION} is installed and established"
      - name: Rotate the Argo CD admin password off the initial secret
        run: |
          set -euo pipefail
          # argocd-initial-admin-secret holds an auto-generated cluster-admin
          # password in plaintext in a namespace Secret, readable by anyone with
          # `kubectl get secret -n argocd`. It is a bootstrap artifact, not a
          # credential store. Replace it with a value held as a repository
          # secret, and delete the bootstrap secret so there is exactly one
          # source of truth.
          #
          # Argo CD reads the bcrypt hash from argocd-secret key
          # admin.password. htpasswd -B is bcrypt; -C 10 matches Argo's own
          # default cost. Writing admin.passwordMtime alongside it is REQUIRED:
          # Argo compares that timestamp against issued JWTs and invalidates
          # sessions older than the change, so omitting it would leave tokens
          # minted under the OLD password valid indefinitely.
          #
          # With the UI now reachable from any source address, this password is
          # the ONLY barrier in front of cluster-admin authority. Keep the
          # ARGOCD_ADMIN_PASSWORD secret long and unique, and rotate it here.
          if ! command -v htpasswd >/dev/null 2>&1; then
            sudo apt-get update -qq
            sudo apt-get install -y -qq apache2-utils
          fi
          HASH="$(htpasswd -nbBC 10 "" "$ARGOCD_ADMIN_PASSWORD" | tr -d ':\n' | sed 's/^\$2y/\$2a/')"
          MTIME="$(date -u +%FT%TZ)"
          kubectl -n argocd patch secret argocd-secret \
            -p "{\"stringData\":{\"admin.password\":\"${HASH}\",\"admin.passwordMtime\":\"${MTIME}\"}}"
          # Idempotent: absent on every run after the first.
          kubectl -n argocd delete secret argocd-initial-admin-secret --ignore-not-found
          # argocd-server caches the credential; restart so the new hash is live
          # before the verify stage or the operator tries to log in.
          kubectl -n argocd rollout restart deployment argocd-server
          kubectl -n argocd rollout status deployment argocd-server --timeout=5m
          echo "admin password rotated; argocd-initial-admin-secret removed"
      - name: Install AWS Load Balancer Controller
        run: |
          set -euo pipefail
          kubectl apply --server-side -f https://raw.githubusercontent.com/aws/eks-charts/v0.0.187/stable/aws-load-balancer-controller/crds/crds.yaml
          helm repo add eks https://aws.github.io/eks-charts
          helm repo update
          helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller \
            --namespace kube-system \
            --version 1.10.1 \
            --set clusterName="$(cat /tmp/cluster_name)" \
            --set region="$AWS_REGION" \
            --set vpcId="$(cat /tmp/vpc_id)" \
            --set serviceAccount.create=true \
            --set serviceAccount.name=aws-load-balancer-controller \
            --set serviceAccount.annotations."eks\.amazonaws\.com/role-arn"="$(cat /tmp/albc_role_arn)" \
            --wait --timeout 10m
      - name: Create the Argo CD UI ingress once the certificate is issued
        run: |
          set -euo pipefail
          STATUS="$(cat /tmp/acm_status)"
          HOSTNAME="$(cat /tmp/argocd_hostname)"
          if [ "$STATUS" != "ISSUED" ]; then
            # PHASE 1. Attaching an unissued certificate to an ALB listener
            # fails, so skip the ingress rather than failing the deploy. The
            # provision stage printed the validation CNAME to add in cPanel.
            # This is a normal, successful outcome of the first deploy.
            echo "ACM certificate status is ${STATUS}, not ISSUED."
            echo "Skipping the Argo CD ingress - it will be created on the next"
            echo "deploy, once the validation CNAME is live and ACM has issued."
            echo "Use kubectl port-forward for UI access in the meantime:"
            echo "  kubectl -n argocd port-forward svc/argocd-server 8080:443"
            exit 0
          fi
          # PHASE 2. Certificate is issued; create the front door.
          #
          # FORMAT CONVERSION. The ARGOCD_ALLOWED_CIDRS secret holds HCL list
          # syntax (["1.2.3.4/32"]) because terraform consumes the same secret
          # as TF_VAR_argocd_allowed_cidrs. The ALB annotation needs a bare
          # comma-separated string. Strip brackets, quotes and spaces.
          ARGOCD_CIDR_LIST="$(printf '%s' "$ARGOCD_ALLOWED_CIDRS" \
            | tr -d '[]" ' )"
          # Refuse to render an ingress with an EMPTY allowlist. This check is
          # NOT about how wide the list is - it is about the list being absent.
          # The ALB controller treats a missing inbound-cidrs annotation as "no
          # restriction", so an unset or malformed secret would silently produce
          # an open console that nobody decided on. An explicitly configured
          # 0.0.0.0/0 is a decision and is allowed through (see below); an empty
          # value is a mistake and fails here.
          if [ -z "$ARGOCD_CIDR_LIST" ]; then
            echo "ERROR: the resolved CIDR allowlist is empty."
            echo "Refusing to create an internet-facing ingress for the Argo CD"
            echo "admin UI without an explicit source list. Check that the"
            echo "ARGOCD_ALLOWED_CIDRS secret is set to an HCL list, e.g."
            echo '  ["203.0.113.10/32"]'
            exit 1
          fi
          # 0.0.0.0/0 USED TO BE REFUSED HERE, mirroring a matching validation
          # rule on the terraform variable. Both were removed on 2026-09-05 on
          # the operator's explicit instruction, after the exposure and the two
          # safer alternatives (a second allowlisted CIDR, or SSO/OIDC with
          # admin.enabled=false) were presented and declined. See the ACCEPTED
          # RISK note in infra/variables.tf. Announce the exposure in the log so
          # it stays visible on every deploy rather than becoming invisible
          # default behaviour.
          case ",$ARGOCD_CIDR_LIST," in
            *,0.0.0.0/0,*)
              echo "NOTICE: the Argo CD admin UI is being exposed to 0.0.0.0/0."
              echo "Every source address on the internet can reach the login"
              echo "form. The ARGOCD_ADMIN_PASSWORD secret is the only barrier"
              echo "in front of cluster-admin authority over this cluster."
              echo "This is an accepted operator decision, not a default."
              echo "The durable fix is SSO/OIDC with admin.enabled=false."
              ;;
          esac
          export ARGOCD_HOSTNAME="$HOSTNAME"
          export ARGOCD_CERT_ARN="$(cat /tmp/acm_arn)"
          export ARGOCD_CIDR_LIST
          export PROJECT_NAME
          envsubst < gitops/argocd-ingress.yaml | kubectl apply -f -
          echo "Argo CD ingress applied for ${HOSTNAME}"
      - name: Confirm the GitOps repository is anonymously readable
        run: |
          set -euo pipefail
          # This repository is PUBLIC by deliberate choice, so Argo CD clones it
          # anonymously and no repository-credential Secret exists. Fail fast and
          # loudly here if that assumption ever stops holding - otherwise the
          # symptom appears minutes later as an opaque "Unknown" Application.
          if ! git ls-remote "${GITOPS_REPO_URL}.git" HEAD >/dev/null 2>&1; then
            echo "ERROR: ${GITOPS_REPO_URL} is not readable without credentials."
            echo "Either make the repository public again, or add a read-only"
            echo "GITOPS_PAT secret and restore the repository-credential step."
            exit 1
          fi
          echo "OK: repository is anonymously readable"
      - name: Apply app-of-apps root Application
        run: |
          set -euo pipefail
          # Fail loudly if the image stage's write-back SHA did not arrive.
          # An empty targetRevision renders as `targetRevision: ""`, which Argo
          # treats as HEAD of the default branch - it would probably work, and
          # that is exactly the problem: an invisible fallback to "whatever main
          # happens to be" is how a deploy silently ships the wrong version.
          if [ -z "${GITOPS_TARGET_REVISION:-}" ]; then
            echo "ERROR: GITOPS_TARGET_REVISION is empty."
            echo "It comes from needs.image.outputs.gitops_sha, set by the image"
            echo "stage's write-back step. An empty value means that step did"
            echo "not run or did not export its output."
            exit 1
          fi
          echo "pinning the root Application to ${GITOPS_TARGET_REVISION}"
          export GITOPS_REPO_URL GITOPS_TARGET_REVISION
          envsubst < gitops/root-app.yaml | kubectl apply -f -
      - name: Wait for every Application to converge on the committed image tag
        run: |
          set -euo pipefail
          # THIS REPLACES THREE STEPS that the patch mechanism required: a
          # `kubectl patch` loop, a repo-server cache-bust restart, and an
          # assertion that the patch had not been reverted.
          #
          # None of them are needed now. The tag is committed in
          # gitops/apps/values.yaml, root is pinned to that exact commit, and
          # Argo renders the children from Git - so the parameters cannot be
          # "reverted", there is no stale failed render to bust (the first
          # render already carries a valid tag), and nothing writes to the
          # cluster that Git disagrees with.
          #
          # What remains is an honest WAIT: confirm each Application actually
          # reports the tag we committed. This proves the loop closed rather
          # than assuming it did.
          EXPECTED="$GITHUB_SHA"
          for APP in app-dev app-bluegreen app-canary; do
            echo "--- $APP (expecting image.tag=${EXPECTED})"
            DEADLINE=$((SECONDS + 420))
            until kubectl get application "$APP" -n argocd >/dev/null 2>&1; do
              if [ "$SECONDS" -ge "$DEADLINE" ]; then
                echo "ERROR: the root Application did not create ${APP} in time."
                kubectl get applications -n argocd
                kubectl get application root -n argocd -o jsonpath='{.status.conditions}'
                exit 1
              fi
              sleep 10
            done
            while true; do
              TAG="$(kubectl get application "$APP" -n argocd \
                -o jsonpath='{.spec.source.helm.parameters[?(@.name=="image.tag")].value}' 2>/dev/null || true)"
              if [ "$TAG" = "$EXPECTED" ]; then
                echo "  OK: ${APP} is on ${EXPECTED}"
                break
              fi
              if [ "$SECONDS" -ge "$DEADLINE" ]; then
                echo "ERROR: ${APP} reports image.tag='${TAG:-<empty>}', expected ${EXPECTED}."
                echo "root is pinned to ${GITOPS_TARGET_REVISION}; check that the"
                echo "write-back commit contains that tag and that root synced it."
                kubectl get application root -n argocd -o jsonpath='{.status.conditions}'
                echo
                kubectl get application "$APP" -n argocd -o jsonpath='{.status.conditions}'
                exit 1
              fi
              sleep 15
            done
          done
          echo "all applications converged on ${EXPECTED} from Git"

  - id: verify
    kind: verify
    needs: [provision, configure]
    timeout_minutes: 30
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_aws_region: us-east-1
      TF_VAR_argocd_allowed_cidrs: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
      ARGOCD_ALLOWED_CIDRS: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
    steps:
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8
      - name: Set up kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: v1.33.0
      - name: Read cluster name from terraform state
        run: |
          set -euo pipefail
          terraform init -input=false -reconfigure \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ secrets.PROJECT_NAME }}/terraform.tfstate" \
            -backend-config="region=us-east-1"
          terraform output -raw cluster_name > /tmp/cluster_name
          terraform output -raw acm_certificate_status > /tmp/acm_status
          terraform output -raw argocd_hostname > /tmp/argocd_hostname
        working-directory: infra
      - name: Update kubeconfig
        run: aws eks update-kubeconfig --region "$AWS_REGION" --name "$(cat /tmp/cluster_name)"
      - name: Wait for Argo CD Applications to be Synced and Healthy
        run: |
          set -euo pipefail
          # ARGO REPORTS A PAUSED ROLLOUT AS health=Suspended, NOT Healthy.
          #
          # A blue/green Rollout with autoPromotionEnabled=false rests at
          # `Paused` on every revision until a human promotes it, and Argo
          # propagates that to the owning Application as `Suspended`. Demanding
          # Healthy from app-bluegreen therefore times out on a deploy that
          # worked perfectly - which is exactly what happened on 2026-09-05.
          #
          # `Suspended` is a DISTINCT status from `Degraded`; the Degraded
          # branch below still fails the job, so a genuinely broken Rollout is
          # caught. The Rollout-level step that follows re-checks these two
          # applications against the controller's own phase.
          #
          # SCOPED DELIBERATELY: root and app-dev own no Rollout and can never
          # legitimately be Suspended, so they stay strictly Healthy and a real
          # hang there is still caught.
          for APP in root app-dev app-bluegreen app-canary; do
            case "$APP" in
              app-bluegreen|app-canary) ROLLOUT_BACKED=true ;;
              *)                        ROLLOUT_BACKED=false ;;
            esac
            echo "waiting for Application/$APP"
            DEADLINE=$((SECONDS + 600))
            while true; do
              SYNC="$(kubectl get application "$APP" -n argocd -o jsonpath='{.status.sync.status}' 2>/dev/null || echo Unknown)"
              HEALTH="$(kubectl get application "$APP" -n argocd -o jsonpath='{.status.health.status}' 2>/dev/null || echo Unknown)"
              echo "  $APP sync=$SYNC health=$HEALTH"
              if [ "$SYNC" = "Synced" ] && [ "$HEALTH" = "Healthy" ]; then
                break
              fi
              if [ "$SYNC" = "Synced" ] && [ "$HEALTH" = "Suspended" ] \
                 && [ "$ROLLOUT_BACKED" = "true" ]; then
                echo "  OK: $APP is Suspended - a Rollout is paused awaiting"
                echo "      promotion. The rollout check below confirms it."
                break
              fi
              if [ "$HEALTH" = "Degraded" ]; then
                echo "Application $APP is Degraded"
                kubectl describe application "$APP" -n argocd
                exit 1
              fi
              if [ "$SECONDS" -ge "$DEADLINE" ]; then
                echo "timed out waiting for $APP (sync=$SYNC health=$HEALTH)"
                # sync=Unknown means Argo could not RENDER the source. The
                # ComparisonError condition carries the helm template error and
                # is far more useful than the describe output alone.
                kubectl get application "$APP" -n argocd -o jsonpath='{.status.conditions}'
                echo
                kubectl describe application "$APP" -n argocd
                exit 1
              fi
              sleep 15
            done
          done
      - name: Confirm the Rollout workloads reached a stable state
        run: |
          set -euo pipefail
          # A blue/green Rollout with autoPromotionEnabled=false PAUSES at
          # `Paused` on its FIRST revision by design, waiting for a human
          # promotion. Treat Healthy and Paused as acceptable resting states and
          # fail only on Degraded - otherwise every first deploy of the
          # blue/green instance would "fail" while behaving exactly as designed.
          #
          # This is the AUTHORITATIVE check for the two Rollout-backed
          # namespaces: it reads the controller's own phase rather than Argo's
          # aggregated health, so a Rollout that is stuck for a real reason
          # (bad image, missing Service) still fails the deploy here.
          for NS in app-bluegreen app-canary; do
            echo "--- namespace $NS"
            DEADLINE=$((SECONDS + 600))
            while true; do
              PHASE="$(kubectl get rollout -n "$NS" \
                -o jsonpath='{.items[0].status.phase}' 2>/dev/null || true)"
              echo "  rollout phase=${PHASE:-<none>}"
              case "$PHASE" in
                Healthy|Paused)
                  echo "  OK ($PHASE)"
                  break
                  ;;
                Degraded)
                  echo "rollout in $NS is Degraded"
                  kubectl describe rollout -n "$NS"
                  kubectl get pods -n "$NS"
                  exit 1
                  ;;
              esac
              if [ "$SECONDS" -ge "$DEADLINE" ]; then
                echo "timed out waiting for the rollout in $NS (phase=${PHASE:-<none>})"
                kubectl describe rollout -n "$NS"
                kubectl get pods -n "$NS"
                exit 1
              fi
              sleep 15
            done
          done
          echo
          echo "Blue/green promotion is MANUAL by design. To promote:"
          echo "  kubectl argo rollouts promote <rollout> -n app-bluegreen"
      - name: Resolve ingress endpoint and curl the service
        run: |
          set -euo pipefail
          DEADLINE=$((SECONDS + 600))
          while true; do
            HOST="$(kubectl get ingress -n app-dev -o jsonpath='{.items[0].status.loadBalancer.ingress[0].hostname}' 2>/dev/null || true)"
            if [ -n "$HOST" ]; then
              break
            fi
            if [ "$SECONDS" -ge "$DEADLINE" ]; then
              echo "timed out waiting for ingress hostname"
              kubectl get ingress -n app-dev
              exit 1
            fi
            sleep 15
          done
          echo "ingress hostname: $HOST"
          curl --fail --show-error --silent --retry 20 --retry-delay 15 --retry-all-errors \
            "http://${HOST}/actuator/health"
      - name: Report the Argo CD UI endpoint
        run: |
          set -euo pipefail
          STATUS="$(cat /tmp/acm_status)"
          HOSTNAME="$(cat /tmp/argocd_hostname)"
          if [ "$STATUS" != "ISSUED" ]; then
            echo "Argo CD UI ingress not created yet (certificate ${STATUS})."
            echo "Add the validation CNAME printed by the provision stage, then"
            echo "run the deploy again. Until then, reach the UI with:"
            echo "  kubectl -n argocd port-forward svc/argocd-server 8080:443"
            exit 0
          fi
          # Wait for the ALB controller to publish the load balancer hostname,
          # then report BOTH names: the ALB hostname is what the operator must
          # CNAME to in cPanel.
          DEADLINE=$((SECONDS + 600))
          while true; do
            ALB="$(kubectl get ingress argocd-server -n argocd -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || true)"
            if [ -n "$ALB" ]; then
              break
            fi
            if [ "$SECONDS" -ge "$DEADLINE" ]; then
              echo "timed out waiting for the Argo CD ALB hostname"
              kubectl describe ingress argocd-server -n argocd
              exit 1
            fi
            sleep 15
          done
          # Report the ACTUAL exposure rather than a fixed sentence. Claiming
          # the console is allowlisted when it is open to the internet would be
          # a false assurance printed on every deploy.
          CIDRS="$(printf '%s' "$ARGOCD_ALLOWED_CIDRS" | tr -d '[]" ')"
          echo "==================================================================="
          echo "Argo CD UI: https://${HOSTNAME}"
          echo "ALB hostname: ${ALB}"
          echo
          echo "If https://${HOSTNAME} does not resolve yet, add this in cPanel:"
          echo "  Type  : CNAME"
          echo "  Name  : ${HOSTNAME%%.*}"
          echo "  Value : ${ALB}"
          echo
          case ",$CIDRS," in
            *,0.0.0.0/0,*)
              echo "EXPOSURE: reachable from ANY source address on the internet."
              echo "The admin password is the only barrier in front of"
              echo "cluster-admin authority. Accepted operator decision."
              ;;
            *)
              echo "Reachable ONLY from the allowlisted source CIDRs. Any other"
              echo "source is refused by the ALB security group and will simply"
              echo "time out - that is the control working, not an outage."
              ;;
          esac
          echo "==================================================================="

rollback:
  strategy: manual

# ---------------------------------------------------------------------------
# EXTRA WORKFLOWS
#
# Rendered to .github/workflows/<id>.yml as standalone workflow_dispatch
# workflows. No provision/configure/verify backbone is required here.
# ---------------------------------------------------------------------------
pipelines:
  - id: rollback
    stages:
      - id: rollback
        kind: custom
        needs: []
        timeout_minutes: 20
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: us-east-1
          TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
          TF_VAR_aws_region: us-east-1
          TF_VAR_argocd_allowed_cidrs: ${{ secrets.ARGOCD_ALLOWED_CIDRS }}
          PROJECT_NAME: ${{ secrets.PROJECT_NAME }}
          GITOPS_REPO_URL: ${{ github.server_url }}/${{ github.repository }}
          # HOW THE OPERATOR SUPPLIES THE TARGET.
          #
          # Through REPOSITORY VARIABLES, not workflow_dispatch inputs. The
          # platform renders every workflow with a bare `workflow_dispatch: {}`
          # and the pipeline spec has no field for declaring `inputs:` - so
          # `${{ inputs.* }}` would silently resolve to an empty string on every
          # run and this workflow could never roll anything back.
          #
          # Repository variables are the mechanism that DOES work from the spec:
          #   Settings -> Secrets and variables -> Actions -> Variables
          #     ROLLBACK_IMAGE_TAG   (required) the commit SHA to roll back to
          #     ROLLBACK_APPLICATION (optional) app-dev | app-bluegreen | app-canary
          #                                     defaults to app-dev
          # Set the variable, then run this workflow. Both are plain config, not
          # credentials, so a variable (visible, editable, auditable) is the
          # right home for them rather than a secret.
          ROLLBACK_APP: ${{ vars.ROLLBACK_APPLICATION }}
          ROLLBACK_TAG: ${{ vars.ROLLBACK_IMAGE_TAG }}
        steps:
          - name: Set up Terraform
            uses: hashicorp/setup-terraform@v3
            with:
              terraform_version: 1.9.8
          - name: Set up kubectl
            uses: azure/setup-kubectl@v4
            with:
              version: v1.33.0
          - name: Resolve and validate the rollback target
            run: |
              set -euo pipefail
              # Default the application so the common case needs ONE variable.
              APP="${ROLLBACK_APP:-app-dev}"
              if [ -z "${ROLLBACK_TAG:-}" ]; then
                echo "ERROR: the ROLLBACK_IMAGE_TAG repository variable is not set."
                echo
                echo "Set it under:"
                echo "  Settings -> Secrets and variables -> Actions -> Variables"
                echo
                echo "  ROLLBACK_IMAGE_TAG   = <the commit SHA to roll back to>"
                echo "  ROLLBACK_APPLICATION = app-dev | app-bluegreen | app-canary"
                echo "                         (optional, defaults to app-dev)"
                echo
                echo "Then run this workflow again."
                exit 1
              fi
              # Map the application to the values.yaml key holding its tag.
              case "$APP" in
                app-dev)       KEY="imageTagDev" ;;
                app-bluegreen) KEY="imageTagBluegreen" ;;
                app-canary)    KEY="imageTagCanary" ;;
                *)
                  echo "ERROR: ROLLBACK_APPLICATION must be one of"
                  echo "  app-dev, app-bluegreen, app-canary"
                  echo "Got: '${APP}'"
                  exit 1
                  ;;
              esac
              echo "APP=${APP}" >> "$GITHUB_ENV"
              echo "TAG_KEY=${KEY}" >> "$GITHUB_ENV"
              echo "rollback target: ${APP} (${KEY}) -> ${ROLLBACK_TAG}"
          - name: Read infrastructure outputs from terraform state
            run: |
              set -euo pipefail
              # Self-sufficient job (this workflow has no provision stage to
              # inherit from): same backend flags, read what we need from state.
              terraform init -input=false -reconfigure \
                -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
                -backend-config="key=${{ secrets.PROJECT_NAME }}/terraform.tfstate" \
                -backend-config="region=us-east-1"
              terraform output -raw cluster_name > /tmp/cluster_name
            working-directory: infra
          - name: Confirm the requested tag exists in ECR
            run: |
              set -euo pipefail
              # FAIL BEFORE COMMITTING ANYTHING. Rolling the repository to a tag
              # that was never pushed produces ImagePullBackOff on every new pod
              # - a broken deployment created BY the rollback that was meant to
              # fix one. Checking ECR first turns that into a clean refusal with
              # nothing written to Git.
              if ! aws ecr describe-images --region "$AWS_REGION" \
                     --repository-name "$PROJECT_NAME" \
                     --image-ids imageTag="$ROLLBACK_TAG" >/dev/null 2>&1; then
                echo "ERROR: no image tagged '${ROLLBACK_TAG}' exists in ECR."
                echo "Recently pushed tags (oldest to newest):"
                aws ecr describe-images --region "$AWS_REGION" \
                  --repository-name "$PROJECT_NAME" \
                  --query 'sort_by(imageDetails,&imagePushedAt)[-20:].imageTags[0]' \
                  --output text
                exit 1
              fi
              echo "OK: ${ROLLBACK_TAG} exists in ECR"
          - name: Commit the rollback to gitops/apps/values.yaml
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            run: |
              set -euo pipefail
              # A ROLLBACK IS A COMMIT, not a patch against the cluster.
              #
              # This used to `kubectl patch` the live Application, which left
              # Git still naming the broken version: the next deploy - or any
              # selfHeal pass - would quietly roll forward again onto the exact
              # image the operator had just backed away from. Writing the old
              # tag to Git makes the rollback durable and auditable, and Argo CD
              # applies it the same way it applies every other change.
              PREVIOUS="$(grep -E "^${TAG_KEY}:" gitops/apps/values.yaml \
                | sed -E 's/^[^:]+:[[:space:]]*"?([^"]*)"?[[:space:]]*$/\1/')"
              echo "rolling ${APP}: ${PREVIOUS:-<unset>} -> ${ROLLBACK_TAG}"
              if [ "$PREVIOUS" = "$ROLLBACK_TAG" ]; then
                echo "${APP} already names ${ROLLBACK_TAG} - nothing to roll back."
                exit 0
              fi
              # Rewrite only this environment's key, leaving the other two
              # untouched. python rather than sed: the replacement is inserted
              # literally, and sed's & and / are landmines here (pitfall #11).
              python3 - "$TAG_KEY" "$ROLLBACK_TAG" <<'PY'
              import re, sys
              key, tag = sys.argv[1], sys.argv[2]
              path = "gitops/apps/values.yaml"
              src = open(path).read()
              new, n = re.subn(rf'^{re.escape(key)}:.*$', f'{key}: "{tag}"',
                               src, count=1, flags=re.M)
              if n != 1:
                  sys.exit(f"ERROR: key {key} not found in {path}")
              open(path, "w").write(new)
              print(f"set {key} to {tag}")
              PY
              git config user.name "github-actions[bot]"
              git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
              git add gitops/apps/values.yaml
              git commit -m "rollback: ${APP} to ${ROLLBACK_TAG} (was ${PREVIOUS:-unset})"
              git pull --rebase origin "${GITHUB_REF_NAME}"
              git push origin "HEAD:${GITHUB_REF_NAME}"
              echo "ROLLBACK_SHA=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
              {
                echo "### Rollback"
                echo ""
                echo "| field | value |"
                echo "| --- | --- |"
                echo "| application | \`${APP}\` |"
                echo "| values key | \`${TAG_KEY}\` |"
                echo "| previous tag | \`${PREVIOUS:-<unset>}\` |"
                echo "| rolled to | \`${ROLLBACK_TAG}\` |"
                echo "| commit | \`$(git rev-parse HEAD)\` |"
                echo ""
                echo "To undo, set ROLLBACK_IMAGE_TAG to the previous tag and re-run."
              } >> "$GITHUB_STEP_SUMMARY"
          - name: Wait for Argo CD to reconcile the rollback
            run: |
              set -euo pipefail
              # The root Application is PINNED to the SHA the last deploy wrote,
              # so it will not pick up this new commit on its own. Repoint it at
              # the rollback commit - the same mechanism the configure stage
              # uses, which is what makes the pin safe rather than a trap.
              #
              # This is the ONE kubectl write this workflow performs, and it is
              # on the root's targetRevision (a pointer to Git), NOT on an image
              # tag. The tag itself still comes from the commit.
              if [ -z "${ROLLBACK_SHA:-}" ]; then
                echo "no rollback commit was made - nothing to reconcile"
                exit 0
              fi
              aws eks update-kubeconfig --region "$AWS_REGION" \
                --name "$(cat /tmp/cluster_name)"
              kubectl -n argocd patch application root --type merge \
                --patch "{\"spec\":{\"source\":{\"targetRevision\":\"${ROLLBACK_SHA}\"}}}"
              echo "root repointed to ${ROLLBACK_SHA}"
              # A ROLLBACK OF A ROLLOUT-BACKED ENVIRONMENT RESTS AT Suspended.
              # Same reason as the verify stage: a paused blue/green Rollout is
              # reported by Argo as health=Suspended, not Healthy. Accept it for
              # the two Rollout-backed applications and let the promotion note
              # below tell the operator what to do next.
              case "$APP" in
                app-bluegreen|app-canary) ROLLOUT_BACKED=true ;;
                *)                        ROLLOUT_BACKED=false ;;
              esac
              DEADLINE=$((SECONDS + 600))
              while true; do
                TAG="$(kubectl get application "$APP" -n argocd \
                  -o jsonpath='{.spec.source.helm.parameters[?(@.name=="image.tag")].value}' 2>/dev/null || true)"
                SYNC="$(kubectl get application "$APP" -n argocd -o jsonpath='{.status.sync.status}' 2>/dev/null || echo Unknown)"
                HEALTH="$(kubectl get application "$APP" -n argocd -o jsonpath='{.status.health.status}' 2>/dev/null || echo Unknown)"
                echo "  tag=${TAG:-<empty>} sync=$SYNC health=$HEALTH"
                if [ "$TAG" = "$ROLLBACK_TAG" ] && [ "$SYNC" = "Synced" ] && [ "$HEALTH" = "Healthy" ]; then
                  echo "rollback complete: ${APP} is Synced and Healthy on ${ROLLBACK_TAG}"
                  break
                fi
                if [ "$TAG" = "$ROLLBACK_TAG" ] && [ "$SYNC" = "Synced" ] \
                   && [ "$HEALTH" = "Suspended" ] && [ "$ROLLOUT_BACKED" = "true" ]; then
                  echo "rollback applied: ${APP} is on ${ROLLBACK_TAG} and paused"
                  echo "awaiting promotion - see the note below."
                  break
                fi
                if [ "$HEALTH" = "Degraded" ]; then
                  echo "ERROR: ${APP} is Degraded after the rollback."
                  kubectl describe application "$APP" -n argocd
                  exit 1
                fi
                if [ "$SECONDS" -ge "$DEADLINE" ]; then
                  echo "timed out (tag=${TAG:-<empty>} sync=$SYNC health=$HEALTH)"
                  kubectl get application "$APP" -n argocd -o jsonpath='{.status.conditions}'
                  exit 1
                fi
                sleep 15
              done
              echo
              echo "NOTE for app-bluegreen: a rollback creates a new PREVIEW"
              echo "ReplicaSet on the old tag and waits for promotion, because"
              echo "autoPromotionEnabled is false. Promote it to take traffic:"
              echo "  kubectl argo rollouts promote <rollout> -n app-bluegreen"
